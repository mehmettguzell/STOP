package com.stop.identity_service;

import org.junit.jupiter.api.Test;

class IdentityServiceApplicationTests {

	@Test
	void contextLoads() {
		// Integration test — requires running infrastructure (PostgreSQL, Kafka).
		// Run separately with the full docker-compose stack.
	}

}
